# Decimal
age = 12
# Binary -> 0 -> 1 -> 10 -> binary
number_ten = 0b1010 # prefix 0b as to covert into binary
# Oct
c = 0o130 # prefix 0o as to covert into oct
# Hex
d = 0x12c # prefix 0x and c at end as to covert into binary

#String
name = "abc"
name2 = 'abc'
mul_string_line = """This is multi line string we can have lines which you can type anything like story"""

# Boolean Literals
x = True
y = False
# == -> Compare operator 1 == True
v1 = (1 == False)
print(v1)